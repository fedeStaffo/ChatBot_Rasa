version: "3.1"

rules:
  - rule: Fuori contesto
    steps:
    - intent: out_of_scope
    - action: utter_out_of_scope

  - rule: Fallback predetto da FallbackClassifier
    steps:
    - intent: nlu_fallback
    - action: utter_fallback


